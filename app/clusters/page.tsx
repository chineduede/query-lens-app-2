'use client';

import React, { useState } from 'react';
import { ChevronDown, ChevronUp, Database, CheckCircle, AlertCircle, ExternalLink } from 'lucide-react';
import { Card, CardHeader, CardTitle, CardContent } from '../components/Card'

export default function CustomerClusters () {
  // fetch list of accounts for tenant
  // fetch list of clusters for each account
  return (
    <h1>Customer Clusters</h1>
  );
};
